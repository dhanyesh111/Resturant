name: restaurant
description: "Restaurant App"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.6.5

  flutter_screenutil:
  intl:
  flutter_svg:
  get_storage: 2.1.1
  cached_network_image:
  google_maps_flutter: ^2.7.0
  geolocator: ^12.0.0
  firebase_core: 
  firebase_storage:
  flutter_polyline_points:



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  
flutter:

  uses-material-design: true

  
  assets:
     - assets/images/
     - assets/icons/
     - assets/map_theme/mapTheme.json

  